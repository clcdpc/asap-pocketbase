# Asap.Migration Slice 0

The migration artifact is permanently separate from the web deployment. Slice
0 establishes its executable identity, source baseline, schema contract, and
self-contained `win-x64` packaging only. It intentionally has no entity export,
import, or reconciliation commands before those entity contracts exist.

Use `describe-contract` for a machine-readable statement of the implemented
surface.
