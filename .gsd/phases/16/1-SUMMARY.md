# Plan 16.1 Summary

- Added `additional-copy-timeout-enabled` and `additional-copy-timeout-days` to `pb_public/staff/index.html`.
- Added population, serialization, save extraction, and toggle event listeners for these fields in `pb_public/staff/js/settings.js`.
