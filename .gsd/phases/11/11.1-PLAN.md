---
phase: 11
plan: 1
wave: 1
---

# Plan 11.1: Grid Column Optimization

## Objective
Remove the redundant "Timing" column from "Pending hold" and "Hold placed" tabs to improve horizontal space efficiency. Re-distribute the freed width to Title and Author columns to enhance readability.

## Context
- .gsd/SPEC.md
- pb_public/staff/js/grid.js

## Tasks

<task type="auto">
  <name>Remove Timing Column from Hold Tabs</name>
  <files>pb_public/staff/js/grid.js</files>
  <action>
    1. In `getGridColumns`, detect if the `status` is `pending_hold` or `hold_placed`.
    2. If so, return a column list that excludes `timingColumn`.
    3. Ensure `outstanding_purchase`, `suggestion`, and `closed` tabs are unaffected (or updated as per user preference). Note: The user specifically mentioned Pending hold and Hold placed.
  </action>
  <verify>
    Check `pb_public/staff/js/grid.js` for the modified `getGridColumns` logic.
  </verify>
  <done>
    The `timingColumn` is no longer returned for `pending_hold` and `hold_placed` statuses.
  </done>
</task>

<task type="auto">
  <name>Adjust Column Widths</name>
  <files>pb_public/staff/js/grid.js</files>
  <action>
    1. Increase `titleColumn` width from `360px` to `410px`.
    2. Increase `authorColumn` width from `230px` to `280px`.
    3. This consumes the `100px` freed by removing the Timing column.
  </action>
  <verify>
    Check the `width` properties of `titleColumn` and `authorColumn` in `grid.js`.
  </verify>
  <done>
    `titleColumn` width is `410px`.
    `authorColumn` width is `280px`.
  </done>
</task>

## Success Criteria
- [ ] "Timing" column is hidden in "Pending hold" and "Hold placed" tabs.
- [ ] Title and Author columns have increased width for better visibility.
- [ ] Grid layout remains balanced and responsive.
