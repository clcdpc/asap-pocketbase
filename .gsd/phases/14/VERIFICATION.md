## Phase 14 Verification

### Must-Haves
- [x] Search filter active on additional copy tab — VERIFIED (enabled standard filters in `renderAdditionalCopiesGrid`).
- [x] Claim filter active on additional copy tab — VERIFIED (enabled standard filters in `renderAdditionalCopiesGrid`).
- [x] Status toggle (Open/Closed) on additional copy tab — VERIFIED (added `additional-copy-status-filter` and updated `loadTab` logic).

### Verdict: PASS
