---
phase: 10
plan: 1
wave: 1
---

# Plan 10.1: Harden Audit Trail and Reactive Logic

## Objective
Expand the staff action audit trail to cover all workflow transitions and implement reactive cleanup of stale workflow flags when a BIB ID is verified.

## Context
- .gsd/SPEC.md
- pb_public/staff/js/modals.js
- pb_public/staff/js/grid.js

## Tasks

<task type="auto">
  <name>Expand Audit Trail Descriptions</name>
  <files>
    <file>pb_public/staff/js/modals.js</file>
  </files>
  <action>
    Update `buildPendingAuditPreview` in `modals.js` to include descriptive text for the following actions:
    - `alreadyOwn`: "This request will be marked Already own and move directly to Closed."
    - `silentClose`: "This request will be closed silently and move directly to Closed."
    - `reassign`: "This request will be reassigned to the selected format."
    - Ensure `renderPendingAuditPreview` is called whenever these actions are toggled in the modal.
  </action>
  <verify>
    Check `modals.js` for the presence of the new action descriptions in the `actionDescriptions` object.
  </verify>
  <done>
    All defined action strings in SPEC.md result in a visible audit preview in the Edit Modal.
  </done>
</task>

<task type="auto">
  <name>Implement Reactive Flag Cleanup</name>
  <files>
    <file>pb_public/staff/js/modals.js</file>
  </files>
  <action>
    In `modals.js`, update the logic that runs after a BIB ID is verified (via `lookupEditBibById` or Polaris Search results) to clear stale flags.
    - If a valid BIB ID is present, remove "Hold failed" and "No holdable items" from the row's `workflowTags` preview.
    - This ensures staff see a clean slate when they have found a potential fix.
  </action>
  <verify>
    Search `modals.js` for "No holdable items" cleanup logic and ensure it triggers on BIB verification.
  </verify>
  <done>
    Verifying a BIB ID in the Edit Modal clears "Hold failed" and "No holdable items" badges from the workflow tag preview.
  </done>
</task>

## Success Criteria
- [ ] Every primary action in the Edit Modal displays a clear audit trail message.
- [ ] Verifying a BIB ID clears stale failure/warning badges from the modal preview.
