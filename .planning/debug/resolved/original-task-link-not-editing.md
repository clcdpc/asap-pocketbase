---
status: resolved
trigger: "When clicking the 'Original task' link in the Notes & activity modal, it should open edit mode of the original suggestion."
symptoms:
  expected: "open the original suggestion in edit mode."
  actual: "it just goes to the list in that status."
  errors: "none"
  reproduction: "Go to Additional Copies tab, click notes icon, then click the link"
created: 2026-05-15
updated: 2026-05-15
root_cause: "The app supported navigating to a specific tab (stage) via URL parameters, but it did not handle auto-opening a specific record (request) when the page loaded or the tab changed."
fix: "Implemented `requestedRequestIdFromUrl` in `api.js` to extract the `request` parameter. Updated `announceTabLoaded` in `grid.js` to check for this parameter, find the matching record in `allSuggestions`, and trigger `openEdit` automatically."
verification: "Verified logic: URL parameters are correctly parsed on load, data is fetched and stored in `allSuggestions` before `announceTabLoaded` is called, and the modal is triggered with the correct row data."
files_changed:
  - pb_public/staff/js/api.js
  - pb_public/staff/js/grid.js
---

# Debug Session: original-task-link-not-editing

## Current Focus
- **Hypothesis**: The link generated by `renderAdditionalCopySourceCell` only sets URL parameters but doesn't trigger the `openEdit` modal on page load or via AJAX navigation. (PROVEN)
- **Next Action**: Investigate how `grid.js` or `app.js` handles the `request` and `stage` URL parameters to trigger the edit modal. (DONE)

## Evidence
- 2026-05-15: Initial report. Link navigates but modal doesn't open.
- 2026-05-15: Found that `api.js` only handles `stage`. No code exists to handle `request`.
- 2026-05-15: Implemented auto-open logic in `announceTabLoaded`.

## Eliminated
(none)
